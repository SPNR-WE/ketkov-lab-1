# Лабораторная работа 1/2

## Описание задачи

Рассматривается задача о покрытии множества (Set Covering Problem) в условиях неопределенности.

Цель состоит в выборе такого подмножества доступных множеств, чтобы каждый базовый элемент общего множества был покрыт хотя бы один раз, при минимизации общих затрат.

Формулировка задачи такая:

$$
\min_x \sum_{j=1}^n c_j x_j
$$

При ограничениях:

$$
\sum_{j=1}^n a_{ij} x_j \ge 1, \quad \forall i \in \{1, \dots, m\}, \quad a_{ji} \text{ -- фиксированы}
$$
$$
x_j \in \{0, 1\}, \quad \forall j \in \{1, \dots, n\}
$$

**Где:**
* $n$ — количество доступных подмножеств (кандидатов на выбор).
* $m$ — количество элементов, которые необходимо покрыть.
* $x_j$ — бинарная переменная решения (равна 1, если множество $j$ выбрано, и 0 в противном случае).
* $c_j$ — стоимость выбора множества $j$.
* $a_{ij}$ — бинарный параметр (равен 1, если множество $j$ покрывает элемент $i$, и 0 в противном случае).

## Решения задачи

### Робастная оптимизация

Теория по выводу робастной оптимизации расписана в файле `src/robust/ROBUST.md`, код представлен в той же директории `src/robust`.

Для запуска экспериментов:
```bash
docker compose up robust
```

### Стохастическая оптимизация

Теория по выводу стохастической оптимизации расписана в файле `src/stochastic/STOCHASTIC.md`, код представлен в той же директории `src/stochastic`.

Для запуска экспериментов:
```bash
docker compose up stochastic
```

### #to-do
